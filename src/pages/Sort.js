import React from 'react';

const Sort = ({ match }) => {
   return (
      <div className="SortedTemplate">
         <div className="SortedPosts">포스트들...</div>
      </div>
   );
};

export default Sort;
