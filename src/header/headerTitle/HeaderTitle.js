import React, { Component } from 'react';
import './HeaderTitle.scss';

class HeaderTitle extends Component {
    render() {
        return(
            <div className="HeaderTitle">devlog</div>
        )
    }
}

export default HeaderTitle;